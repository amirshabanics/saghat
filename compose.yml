# Development compose — delegates to docker/compose.yml
# Usage: docker compose -f docker/compose.yml up
# Or from repo root: docker compose --project-directory docker up
include:
  - path: docker/compose.backend.yml
  - path: docker/compose.frontend.yml
